# 설계 결정 기록 (Design Decisions)

본 문서는 프로젝트 진행 중 내려진 주요 설계 결정과 그 근거를 기록한다.
ADR(Architecture Decision Record) 형식을 단순화한 형태로 작성한다.

---

## DD-001: 프레임워크 미사용, Vanilla 스택 채택

- **결정일**: 2026-05-18
- **상태**: Accepted

### 맥락

소프트웨어공학 과제 "프로세스에 입각한 바이브코딩의 효과 논술"의 실증 프로젝트로 데이터 시각화 웹 대시보드를 개발한다. 프레임워크 선정 시 다음 후보를 검토했다.

1. React + Recharts
2. Next.js + D3
3. Vanilla HTML/CSS/JS + Chart.js + Leaflet

### 결정

**3번 (Vanilla 스택)** 채택.

### 근거

| 평가 항목 | Vanilla | React + Recharts | Next.js + D3 |
|---|---|---|---|
| 초기 설정 비용 | 낮음 | 중간 | 높음 |
| Git 커밋 가시성 | 높음 (정적 파일) | 낮음 (빌드 산출물) | 낮음 (빌드 산출물) |
| 보고서 캡처 적합성 | 높음 | 높음 | 높음 |
| AI 도구 안정성 | 높음 (버전 충돌 없음) | 중간 | 중간 |
| 학습 곡선 | 낮음 | 중간 | 높음 |
| MVP 도달 속도 | 빠름 | 중간 | 느림 |

핵심 판단:
- 본 과제의 평가 핵심은 **결과물 품질이 아닌 프로세스 적용 증빙**이다.
- 프레임워크 설정 시간만큼 프로세스 문서화에 투자하는 것이 더 합리적.
- 정적 파일 기반이면 `index.html`, `app.js` 등의 변경 이력이 git history에 직관적으로 남아 채점자가 진행 과정을 따라가기 쉽다.

### 결과

- 빌드 도구 없이 브라우저에서 바로 실행 가능한 구조 채택
- 외부 라이브러리는 CDN으로 로드
- Phase 2에서 Leaflet 도입 시에도 동일 원칙 유지

---

## DD-002: AI 도구 역할 분담 (Claude × Codex)

- **결정일**: 2026-05-18
- **상태**: Accepted

### 맥락

과제 요건상 바이브코딩 AI 도구는 자율 선정 가능하다. 단일 AI 사용 vs 복수 AI 교차 사용을 검토했다.

### 결정

**Claude + Codex 병행 사용, 역할 분담 운용**.

| 도구 | 주 담당 |
|---|---|
| Claude | 요구사항 정리, 문서/보고서 작성, 의사결정 토론, 코드 리뷰 |
| Codex | 코드 구현, 버그 수정, 비판적 리뷰, 커밋 메시지 정리 |

### 근거

- 단일 AI 사용 시 그 AI의 편향이 그대로 결과물에 반영되는 리스크 존재
- 킥오프 단계에서 이미 효과 확인: Claude의 1차 주제 제안(플래시카드)을 Codex가 반박하여 더 적합한 주제(지역 불균형 대시보드)로 수렴함
- 두 AI의 응답 비교 자체가 **lessons learned의 핵심 소재**가 됨

### 결과

- 주요 의사결정 시 두 AI 의견을 모두 청취 후 결론 기록
- 모든 교차 토의는 `docs/ai-log/` 폴더에 날짜별로 누적
- 최종 보고서에 "단일 AI vs 교차 검증" 비교 섹션 포함 예정

---

## DD-003: Phase 기반 점진 개발 + MVP 우선

- **결정일**: 2026-05-18
- **상태**: Accepted

### 맥락

21일이라는 제한된 기간 내에 데이터 시각화 대시보드를 완성해야 한다. 데이터 시각화는 스코프 크리프 위험이 큰 영역이다.

### 결정

4단계 Phase 구조 채택, 각 Phase 종료 시점에 산출물 검증.

| Phase | 목표 | 기간 |
|---|---|---|
| 1 (MVP) | 통계 카드 + 막대차트 | 2026-05-18 ~ 05-24 |
| 2 | Leaflet 시도 단위 지도(choropleth) | 2026-05-25 ~ 05-31 |
| 3 | 폐교 통계 + 필터 + 설명 카드 | 2026-06-01 ~ 06-05 |
| QA / 보고서 | 버그픽스, PDF 작성 | 2026-06-06 ~ 06-08 |

### 근거

- MVP를 Phase 1 끝에 확보하면 이후 Phase가 막혀도 제출 가능한 결과물이 존재
- 지도 시각화(Phase 2)는 난이도가 높아 MVP에서 제외
- 각 Phase 종료 시점이 자연스러운 회고 지점이 되어 lessons learned 누적에 유리

### 결과

- Phase 1 완료 전까지 지도 작업 착수 금지
- Phase 2 지도는 시군구가 아닌 시도 단위로 제한
- 폐교 데이터는 지도 레이어가 아니라 카드와 차트로 표현
- 각 Phase 종료 시 `docs/ai-log/`에 회고 기록

---

## DD-004: API 직접 호출 대신 정적 JSON 데이터 채택

- **결정일**: 2026-05-21
- **상태**: Accepted

### 맥락

Phase 1 대시보드는 KOSIS, 행정안전부, 교육부 등 여러 출처의 데이터를 함께 사용한다. 브라우저에서 API를 직접 호출하는 방식은 API 키 발급, CORS, 호출 제한, 응답 형식 변경 리스크를 동반한다. 과제의 핵심은 실시간 데이터 연동이 아니라 소프트웨어공학 프로세스 적용과 시각화 산출물이다.

### 결정

**원본 CSV를 확보한 뒤 전처리된 정적 JSON을 `data/`에 저장하여 사용한다.**

### 근거

- 정적 JSON은 Git에 커밋할 수 있어 결과 재현성이 높다.
- 브라우저 보안 정책과 외부 API 장애에 영향을 덜 받는다.
- Phase 1의 구현 범위를 카드와 막대차트에 집중할 수 있다.
- 데이터 변환 과정 자체를 문서화하면 요구사항 추적과 검증이 쉬워진다.

### 결과

- Phase 1은 `data/phase1-regions.json`을 기준 데이터로 사용한다.
- 원본 CSV를 확보하면 `data/raw/`에 보관하고, 변환 결과는 `data/processed/` 또는 루트 JSON으로 관리한다.
- 데이터 값의 출처와 임시값 여부는 README 또는 별도 데이터 문서에 명시한다.

---

## DD-005: 위험 기반 반복·점진 개발 프로세스 채택

- **결정일**: 2026-05-25
- **상태**: Accepted

### 맥락

본 프로젝트는 단기간 과제이며, 기능 구현뿐 아니라 소프트웨어공학 프로세스 적용을 증빙해야 한다.
데이터 시각화 프로젝트 특성상 데이터 확보, 지도 구현, 폐교 위치 전처리 등 일정 리스크가 크다.

검토한 접근은 다음과 같다.

1. 선형 폭포수 모델
2. 완전한 애자일 스프린트 모델
3. 위험 기반 반복·점진 개발

### 결정

**위험 기반 반복·점진 개발**을 채택한다.

각 Phase는 작은 단위의 `요구사항 -> 설계 -> 구현 -> 검증 -> 회고` 흐름을 따른다.
단, 전체 프로젝트는 Phase 1 MVP, Phase 2 지도, Phase 3 폐교 통계 확장으로 점진적으로 확장한다.

### 근거

- 폭포수 모델만 사용하면 초기 요구사항 변경과 데이터 소싱 리스크에 대응하기 어렵다.
- 완전한 애자일 스프린트는 1인 과제 규모에 비해 의식과 산출물이 과하다.
- 반복·점진 개발은 제출 가능한 MVP를 먼저 확보하면서도, 지도와 공식 데이터 교체를 후속 Phase로 관리하기 좋다.
- V-Model식 추적성을 함께 적용하면 요구사항과 테스트 기준을 명확히 연결할 수 있다.

### 결과

- `docs/process.md`에 전체 프로세스와 다이어그램을 추가한다.
- 새 기능은 요구사항과 설계 결정 기록을 먼저 갱신한 뒤 구현한다.
- 각 Phase 종료 시 `docs/ai-log/`와 `docs/lessons-learned.md`에 회고를 남긴다.
- 자동화 가능한 검증은 `tests/`에 스크립트로 누적한다.

---

## DD-006: Phase 2 지도 경계는 저장소 내 TopoJSON으로 관리

- **결정일**: 2026-05-25
- **상태**: Accepted

### 맥락

Phase 2 목표는 시도 단위 choropleth 지도다. Leaflet은 GeoJSON과 잘 맞지만, 원본 시도 GeoJSON은
파일 크기가 커서 정적 대시보드에 부담이 된다. 반대로 외부 URL을 매번 호출하면 네트워크 상태와
원격 파일 변경에 영향을 받는다.

### 결정

`southkorea/southkorea-maps`의 KOSTAT 2018 시도 단위 단순 TopoJSON을 `data/`에 저장하고,
브라우저에서는 `topojson-client`로 GeoJSON으로 변환한 뒤 Leaflet에 전달한다.

### 근거

- 단순 TopoJSON은 원본 GeoJSON보다 작아 정적 호스팅에 적합하다.
- 경계 파일을 저장소에 포함하면 제출 시점의 화면을 재현하기 쉽다.
- 데이터 출처와 라이선스를 `data/README.md`와 `data/skorea-provinces-license.md`에 남길 수 있다.
- Phase 2는 시군구가 아니라 시도 단위이므로 17개 경계만 있으면 MVP 요구를 충족한다.

### 결과

- `data/skorea-provinces-2018-topo-simple.json` 추가
- `src/app.js`에서 Phase 1 지역 데이터와 지도 경계 코드를 매핑
- 지도 상세 패널에 인구 증감률, 인구감소지역 수, 폐교 수 표시
- 지도 지표 전환과 지역 선택 인터랙션 추가
- `tests/verify-data.mjs`에서 지도 경계 17개와 코드 매핑을 자동 검증

---

## DD-007: 데이터 표시를 넘어 위험지수 기반 진단 제공

- **결정일**: 2026-05-25
- **상태**: Accepted

### 맥락

지도와 차트만 제공하면 사용자는 "그래서 무엇을 봐야 하는가"를 스스로 해석해야 한다.
본 프로젝트의 목표는 지역 불균형을 일반 사용자가 빠르게 이해하도록 돕는 것이므로,
단순 시각화만으로는 사용자 가치가 약하다.

### 결정

인구 증감률, 인구감소지역 수, 폐교 수를 결합한 **종합 위험지수**를 계산하고,
핵심 진단 카드와 지도 기본 지표로 제공한다.

위험지수는 다음 비중으로 계산한다.

| 요소 | 비중 |
|---|---|
| 인구 감소 폭 | 40% |
| 인구감소지역 수 | 35% |
| 폐교 수 | 25% |

### 근거

- 인구 감소만 보면 지역 기반 시설 축소를 설명하기 어렵다.
- 폐교 수만 보면 인구 변화의 방향성을 놓칠 수 있다.
- 세 지표를 함께 보면 "우선 확인해야 할 지역"을 빠르게 찾을 수 있다.
- 위험지수는 임시 데이터 기반 가설 지표이므로 공식 데이터 교체 후 재보정한다.

### 결과

- `src/app.js`에서 지역별 위험지수와 등급을 계산한다.
- 지도 기본 지표를 위험지수로 변경한다.
- 핵심 진단 카드에 우선 대응 지역, 감소 신호, 수도권/비수도권 격차를 표시한다.
- 위험도 상위 지역 목록을 추가 분석 결과로 제공한다.

---

## DD-008: 위험지수 시나리오 전환 기능 추가

- **결정일**: 2026-05-26
- **상태**: Accepted

### 맥락

위험지수를 하나의 고정 산식으로만 제공하면 사용자는 분석 기준을 검토하거나 비교하기 어렵다.
지역 불균형 문제는 인구 감소, 소멸위험 지정 지역, 교육 인프라 축소 중 무엇을 더 중요하게
보느냐에 따라 우선순위가 달라질 수 있다.

### 결정

위험지수 계산 기준을 네 가지 시나리오로 전환할 수 있게 한다.

| 시나리오 | 인구 감소 | 인구감소지역 | 폐교 수 |
|---|---:|---:|---:|
| 균형 | 40% | 35% | 25% |
| 인구감소 중심 | 70% | 20% | 10% |
| 소멸위험 중심 | 25% | 55% | 20% |
| 폐교 중심 | 20% | 25% | 55% |

### 근거

- 단일 점수보다 "분석 기준에 따른 순위 변화"가 더 설명력 있다.
- 사용자가 정책 관점에 맞춰 우선순위를 비교할 수 있다.
- 위험지수 산식이 임시 데이터 기반이라는 한계를 UI에서 드러낼 수 있다.

### 결과

- 핵심 진단 패널에 시나리오 버튼을 추가한다.
- 시나리오 변경 시 위험지수, 핵심 진단, 위험도 상위 지역, 지도 색상, 상세 패널을 함께 갱신한다.
- 2026-05-26 검증 기록을 `docs/test-plan.md`에 남긴다.

---

## DD-009: 두 지역 직접 비교 모드 추가

- **결정일**: 2026-05-26
- **상태**: Accepted

### 맥락

기존 대시보드는 지도, 차트, 순위로 전체 흐름을 보여주지만 사용자가 특정 두 지역을 직접 비교하는
흐름이 약했다. 사용자는 "전남과 경기는 얼마나 다른가"처럼 구체적인 비교 질문을 가질 수 있다.

### 결정

핵심 진단 아래에 두 지역을 선택하는 비교 모드를 추가한다.

### 근거

- 단순 조회형 화면에서 사용자가 직접 질문을 만드는 탐색형 화면으로 확장된다.
- 위험지수 시나리오 변경 결과가 비교 카드에도 반영되어 분석 흐름이 연결된다.
- 지도에서 지역을 찾기 전에도 관심 지역을 빠르게 비교할 수 있다.

### 결과

- 지역 A, 지역 B 선택 드롭다운 추가
- 위험지수 차이, 인구 흐름 차이, 인구감소지역·폐교 수 차이 카드 추가
- 시나리오 전환 시 비교 결과도 함께 재계산

---

## DD-010: 위험지수 산식 설명 UI 추가

- **결정일**: 2026-05-27
- **상태**: Accepted

### 맥락

위험지수 시나리오와 지역 비교 기능을 추가했지만, 사용자는 점수가 어떤 산식으로 만들어지는지
화면에서 바로 확인하기 어렵다. 파생 지표는 산식과 한계를 숨기면 신뢰도가 낮아진다.

### 결정

핵심 진단 영역에 현재 선택된 위험지수 산식과 요소별 가중치 바를 표시한다.

### 근거

- 점수의 계산 근거를 화면에 드러내면 임의 순위처럼 보이는 위험을 줄일 수 있다.
- 시나리오 버튼과 산식 표시가 함께 갱신되면 사용자가 기준 변화를 즉시 이해할 수 있다.
- 공식 데이터 교체 전까지 위험지수가 가설 지표라는 점을 정규화 안내 문구로 명시할 수 있다.

### 결과

- `FR-017` 요구사항 추가
- 시나리오 전환 시 산식 텍스트와 가중치 바 동시 갱신
- 2026-05-27 검증 기록과 AI 작업 로그 추가

---

## DD-011: 지역 목록을 탐색형 컨트롤로 확장

- **결정일**: 2026-05-31
- **상태**: Accepted

### 맥락

위험도 상위 6개 지역만 보여주는 목록은 빠른 요약에는 좋지만, 사용자가 직접 지역을 찾아보고
정렬 기준을 바꾸는 흐름이 약하다. 지도와 비교 기능 사이에서 17개 시도 전체를 훑는 중간 탐색
도구가 필요하다.

### 결정

지역 목록을 검색, 수도권/비수도권 필터, 정렬 컨트롤을 가진 탐색 패널로 확장한다.

### 근거

- 사용자가 특정 지역명을 직접 찾을 수 있어 데모 조작성이 좋아진다.
- 위험지수, 인구감소, 인구감소지역, 폐교 수 기준 정렬을 제공하면 같은 데이터를 여러 관점으로 볼 수 있다.
- 위험지수 시나리오 변경 시 목록도 재계산되어 기존 분석 흐름과 연결된다.

### 결과

- `FR-018` 요구사항 추가
- 지역 검색 입력, 권역 필터, 정렬 드롭다운 추가
- 지역 목록을 상위 6개 고정이 아니라 필터 결과 전체로 렌더링

---

## DD-012: 위험지수 기반 대응 전략 브리프 추가

- **결정일**: 2026-05-31
- **상태**: Accepted

### 맥락

지도, 비교, 정렬은 사용자가 데이터를 탐색하게 만들지만, 여전히 "그래서 무엇을 해야 하는가"라는
질문에는 직접 답하지 않는다. 대시보드가 제출용 데모를 넘어 분석 도구처럼 보이려면 위험지수 결과를
대응 판단 문장으로 바꿔주는 영역이 필요하다.

### 결정

선택된 위험지수 시나리오의 최상위 위험 지역, 주요 기여 요인, 후속 검증 과제를 카드로 제공한다.

### 근거

- 위험지수의 구성 요소 중 어떤 요인이 가장 큰지 보여주면 점수 해석이 쉬워진다.
- 시나리오 변경 시 대응 전략도 함께 바뀌어 분석 흐름이 끊기지 않는다.
- 공식 시도별 데이터 교체 필요성을 화면 안에서 드러내면 데이터 한계도 관리된다.

### 결과

- `FR-019` 요구사항 추가
- Action Brief 영역 추가
- 위험지수 기여 요인 계산 함수 추가

---

## DD-013: 폐교 통계 전용 분석 패널 추가

- **결정일**: 2026-05-31
- **상태**: Accepted

### 맥락

Phase 3 목표는 폐교 통계와 필터, 설명 카드를 추가하는 것이다. 기존 화면은 폐교 수를 요약 카드,
지도 지표, 지역 목록의 부가 정보로 보여주지만 폐교만 따로 분석하는 흐름은 부족했다.

### 결정

폐교 통계를 별도 패널로 분리하고, 권역/고위험 필터, 전용 막대차트, 설명 카드를 제공한다.

### 근거

- 폐교 수는 지역 불균형의 교육 인프라 축소 신호이므로 독립 분석 화면이 필요하다.
- 지도 색상보다 차트가 폐교 수의 지역 간 격차를 더 직접적으로 보여준다.
- 현재 시도별 폐교 수가 임시 데이터라는 한계를 패널 안내 문구에 명시한다.

### 결과

- `FR-020` 요구사항 추가
- 폐교 통계 분석 패널과 필터 버튼 추가
- Chart.js 기반 폐교 수 가로 막대차트 추가

---

## DD-014: 데이터 신뢰도 상태를 화면에 표시

- **결정일**: 2026-05-31
- **상태**: Accepted

### 맥락

요약 지표는 공식 통계로 교체됐지만 시도별 지도·위험지수 입력값은 아직 임시 데이터다. 이 구분은
문서에만 있으면 데모 화면을 보는 사용자가 놓치기 쉽다.

### 결정

화면 하단에 요약 지표, 시도별 분석, 지도 경계의 신뢰도 상태를 분리한 데이터 품질 패널을 추가한다.

### 근거

- 공식 데이터와 임시 데이터를 명확히 구분하면 과장된 분석처럼 보이는 위험을 줄일 수 있다.
- 최종 보고서에서 데이터 한계와 후속 작업을 설명하기 쉬워진다.
- 상태 배지를 `혼합 데이터`로 바꿔 현재 데이터 단계가 더 정확히 드러난다.

### 결과

- `FR-021` 요구사항 추가
- 데이터 신뢰도 카드 3개 추가
- `summary-official-mixed` 상태를 화면 배지에 반영

---

## DD-015: 보고서 요약 Markdown 내보내기 추가

- **결정일**: 2026-06-01
- **상태**: Accepted

### 맥락

최종 제출 단계에서는 화면 캡처뿐 아니라 현재 앱이 어떤 분석 결과와 데이터 한계를 갖는지
보고서 문장으로 옮겨야 한다. 매번 수동으로 요약하면 화면 상태와 보고서 내용이 어긋날 수 있다.

### 결정

현재 위험지수 시나리오, 핵심 요약 지표, 우선 위험 지역, 데이터 한계를 Markdown 파일로 내보내는
보고서 요약 기능을 추가한다.

### 근거

- 보고서 작성 시간을 줄이고 앱 상태와 문서 내용을 일치시키기 쉽다.
- 위험지수 시나리오 전환 결과가 보고서 요약에도 반영된다.
- 데이터 한계 문구를 자동 포함해 임시 데이터 사용 사실을 숨기지 않는다.

### 결과

- `FR-022` 요구사항 추가
- 보고서 요약 패널과 MD 다운로드 버튼 추가
- 현재 시나리오 기준 보고서 요약 생성 함수 추가

---

## DD-016: GitHub Actions 기반 자동 검증 추가

- **결정일**: 2026-06-01
- **상태**: Accepted

### 맥락

검증 명령은 로컬에서 반복 실행하고 있었지만, GitHub에 푸시된 뒤 자동으로 확인되는 구조는 없었다.
제출 직전에는 작은 HTML id 변경이나 JSON 오류도 놓치기 쉬우므로 원격 저장소에서도 동일한 검증을
반복해야 한다.

### 결정

`npm test`로 로컬 검증 명령을 묶고, GitHub Actions CI에서 push와 pull request마다 실행한다.

### 근거

- 로컬 검증과 원격 검증 명령을 일치시켜 재현성을 높인다.
- 별도 빌드 도구가 없는 정적 앱이므로 데이터 구조, UI 계약, 문법 검증만으로도 회귀를 상당 부분 막을 수 있다.
- 최종 보고서에서 자동 검증 적용 근거로 활용할 수 있다.

### 결과

- `package.json` 검증 스크립트 추가
- `.github/workflows/ci.yml` 추가
- 테스트 계획에 `A-006` 추가

---

## DD-017: GitHub Pages 정적 배포 준비

- **결정일**: 2026-06-01
- **상태**: Accepted

### 맥락

현재 앱은 로컬 정적 서버에서 실행되지만, 최종 제출 시 채점자가 별도 설정 없이 데모를 열 수 있으면
검토 부담이 줄어든다. GitHub Pages는 정적 HTML/CSS/JS 앱과 잘 맞는다.

### 결정

GitHub Actions 기반 Pages 배포 워크플로를 추가하고, 저장소 루트에 `src/index.html`로 이동하는
진입점 `index.html`을 둔다.

### 근거

- 앱은 빌드 산출물이 필요 없는 정적 파일 구조이므로 Pages 배포가 단순하다.
- 루트 진입점이 있으면 Pages 기본 URL에서 바로 앱 화면으로 이동할 수 있다.
- 배포 전에 `npm test`를 실행해 깨진 상태가 Pages에 올라가는 위험을 줄인다.

### 결과

- `.github/workflows/pages.yml` 추가
- 루트 `index.html` 리다이렉트 추가
- `.nojekyll` 추가

---

## DD-018: 시나리오별 위험지역 매트릭스 추가

- **결정일**: 2026-06-01
- **상태**: Accepted

### 맥락

위험지수 시나리오 버튼은 선택한 기준의 결과를 갱신하지만, 네 기준을 한눈에 비교하기는 어렵다.
보고서에서는 "분석 기준에 따라 우선순위가 어떻게 달라지는가"를 설명해야 한다.

### 결정

네 가지 위험지수 시나리오별 상위 3개 지역과 점수를 한 번에 보여주는 매트릭스를 추가한다.

### 근거

- 사용자는 버튼을 반복해서 누르지 않아도 기준별 우선 지역을 비교할 수 있다.
- 위험지수가 단일 정답이 아니라 분석 관점에 따라 달라지는 파생 지표임을 화면에서 드러낸다.
- 보고서 캡처 하나로 시나리오 비교 결과를 설명할 수 있다.

### 결과

- `FR-023` 요구사항 추가
- Scenario Matrix 패널 추가
- 시나리오별 위험지수 계산 helper 추가

---

## DD-019: 보고서 캡처 모드와 인쇄 스타일 추가

- **결정일**: 2026-06-01
- **상태**: Accepted

### 맥락

최종 보고서에는 화면 캡처가 필요하다. 일반 대시보드 화면에는 필터, 버튼, 드롭다운 등 조작 요소가
많아 캡처 시 핵심 분석 결과가 덜 깔끔하게 보일 수 있다.

### 결정

헤더에 캡처 모드 버튼을 추가하고, 캡처 모드 및 인쇄 환경에서는 조작 컨트롤을 숨기고 그림자 효과를
줄이는 스타일을 적용한다.

### 근거

- 보고서 캡처 시 핵심 카드와 차트가 더 잘 보인다.
- 앱 안에서 제출 준비 모드까지 제공하므로 QA/보고서 단계의 작업이 줄어든다.
- 인쇄 스타일을 함께 두면 PDF 캡처나 브라우저 인쇄에도 대응할 수 있다.

### 결과

- `FR-024` 요구사항 추가
- 캡처 모드 토글 버튼 추가
- `capture-mode` 스타일과 `@media print` 스타일 추가
