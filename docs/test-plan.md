# 테스트 계획

본 문서는 Phase 1 MVP와 Phase 2 지도 초안의 검증 기준을 정의한다.
브라우저 기반 정적 대시보드이므로 초기 단계에서는 체크리스트 중심으로 검증한다.

---

## 1. 검증 범위

- 정적 JSON 데이터 로드
- 통계 카드 렌더링
- Chart.js 막대차트 렌더링
- Leaflet 시도 단위 지도 렌더링
- 반응형 레이아웃
- 문서와 구현의 일치 여부
- 데이터 구조 자동 검증
- JavaScript 문법 검증

---

## 2. 자동 검증

본 프로젝트는 별도 빌드 도구가 없는 정적 웹앱이므로, 최소 자동 검증은 Node.js 기반으로 수행한다.

| ID | 명령 | 검증 내용 | 기대 결과 |
|---|---|---|---|
| A-001 | `node --check src/app.js` | JavaScript 문법 오류 확인 | 오류 없이 종료 |
| A-002 | `node tests/verify-data.mjs` | JSON 필수 키, 17개 시도, 지도 경계 17개, 수도권 매핑 확인 | `Data verification passed.` 출력 |
| A-003 | `python -m json.tool data/phase1-regions.json` | JSON 파싱 가능 여부 확인 | 오류 없이 종료 |
| A-004 | `python -m json.tool data/skorea-provinces-2018-topo-simple.json` | TopoJSON 파싱 가능 여부 확인 | 오류 없이 종료 |
| A-005 | `node tests/verify-ui-contract.mjs` | 핵심 DOM id, 시나리오/지도 버튼 수, UI 렌더링 함수 연결 확인 | `UI contract verification passed.` 출력 |

---

## 3. 수동 테스트 체크리스트

| ID | 항목 | 절차 | 기대 결과 |
|---|---|---|---|
| T-001 | 데이터 로드 | 저장소 루트에서 정적 서버를 실행하고 `http://127.0.0.1:8000/src/index.html`을 연다. | JSON 데이터가 로드되고 오류 메시지가 표시되지 않는다. |
| T-002 | 통계 카드 | 카드 영역을 확인한다. | 4개 핵심 카드가 표시된다. |
| T-003 | 차트 렌더링 | 막대차트 영역을 확인한다. | 17개 시도 막대가 표시된다. |
| T-004 | 수도권 구분 | 서울, 인천, 경기 항목을 확인한다. | 수도권 항목이 다른 지역과 구분된다. |
| T-005 | 데이터 기준 표시 | 데이터 설명 영역을 확인한다. | 기준연도와 임시 데이터 안내가 표시된다. |
| T-006 | 모바일 레이아웃 | 좁은 화면 폭에서 확인한다. | 카드와 차트 텍스트가 겹치지 않는다. |
| T-007 | 콘솔 오류 | 브라우저 개발자 도구 콘솔을 확인한다. | 로딩 실패나 JavaScript 오류가 없다. |
| T-008 | 지도 렌더링 | 지도 영역을 확인한다. | 17개 시도 경계가 색상으로 표시된다. |
| T-009 | 지도 상세 정보 | 지도 지역에 마우스를 올리거나 클릭한다. | 해당 지역의 인구 증감률, 인구감소지역 수, 폐교 수가 표시된다. |
| T-010 | 지도 지표 전환 | 지도 지표 버튼을 누른다. | 지도 제목, 범례, 색상 기준이 선택 지표에 맞게 바뀐다. |
| T-011 | 지역 직접 선택 | 지역 선택 드롭다운에서 시도를 선택한다. | 선택한 시도의 상세 정보가 표시되고 지도 영역이 해당 지역으로 이동한다. |
| T-012 | 핵심 진단 | 핵심 진단 영역을 확인한다. | 우선 대응, 감소 신호, 격차 요약 카드가 표시된다. |
| T-013 | 위험지수 상세 | 지역을 선택한다. | 상세 패널에 종합 위험지수와 위험 등급이 표시된다. |
| T-014 | 위험지수 시나리오 전환 | 분석 기준 버튼을 누른다. | 핵심 진단, 위험도 상위 지역, 지도 색상, 상세 위험지수가 재계산된다. |
| T-015 | 지역 비교 | 지역 A와 지역 B를 선택한다. | 두 지역의 위험지수 차이, 인구 흐름 차이, 인구감소지역·폐교 수 차이가 표시된다. |
| T-016 | 위험지수 산식 설명 | 분석 기준 버튼을 누른다. | 산식 텍스트, 정규화 안내, 요소별 가중치 바가 선택 시나리오에 맞게 바뀐다. |
| T-017 | 지역 탐색 | 검색어, 권역 필터, 정렬 기준을 바꾼다. | 지역 목록이 조건에 맞게 필터링되고 선택 정렬 기준으로 재배치된다. |
| T-018 | 대응 전략 브리프 | 분석 기준 버튼을 누른다. | 최상위 위험 지역, 주요 기여 요인, 후속 검증 과제 카드가 선택 시나리오 기준으로 갱신된다. |
| T-019 | 폐교 통계 분석 | 폐교 분석 필터를 누른다. | 폐교 차트, 합계 카드, 최다 지역 카드, 감소지역 동반 카드가 필터 기준에 맞게 갱신된다. |
| T-020 | 데이터 신뢰도 | 데이터 신뢰도 영역을 확인한다. | 요약 지표, 시도별 분석, 지도 경계의 공식/임시/고정 상태가 구분되어 표시된다. |
| T-021 | 보고서 요약 내보내기 | 분석 기준을 바꾸고 요약 MD 버튼을 누른다. | 현재 시나리오와 핵심 분석 결과를 담은 Markdown 파일이 생성된다. |

---

## 4. 완료 기준

- A-001부터 A-005까지 모두 통과한다.
- T-001부터 T-021까지 모두 통과한다.
- 실패 항목은 `docs/ai-log/` 또는 커밋 메시지에 수정 내역을 남긴다.
- Phase별 주요 화면 캡처를 확보한다.

---

## 5. 2026-05-25 검증 기록

| 항목 | 결과 |
|---|---|
| `node --check src/app.js` | 통과 |
| `node tests/verify-data.mjs` | 통과 |
| `python -m json.tool data/phase1-regions.json` | 통과 |
| `python -m json.tool data/skorea-provinces-2018-topo-simple.json` | 통과 |
| 정적 서버 경로 확인 | `src/index.html`, `data/phase1-regions.json`, `data/skorea-provinces-2018-topo-simple.json` 모두 200 OK |

---

## 6. Phase 2 추가 검증 기록

| 항목 | 결과 |
|---|---|
| Leaflet 지도 영역 추가 | 통과 |
| 17개 시도 TopoJSON 경계 연결 | 통과 |
| Phase 1 데이터와 지도 코드 매핑 | 통과 |
| 지도 상세 패널 표시 | 통과 |
| 지도 지표 버튼 3개 표시 | 통과 |
| 지역 선택 옵션 17개 표시 | 통과 |
| 핵심 진단 카드 3개 표시 | 통과 |
| 상세 패널 위험지수 표시 | 통과 |

---

## 7. 2026-05-26 검증 기록

| 항목 | 결과 |
|---|---|
| `node --check src/app.js` | 통과 |
| `node tests/verify-data.mjs` | 통과 |
| 위험지수 시나리오 버튼 4개 표시 | 통과 |
| 지도 지표 버튼 4개 표시 | 통과 |
| 폐교 중심 시나리오 전환 | 통과 |
| 지역 상세 위험지수 재계산 | 통과 |
| 지역 비교 카드 3개 표시 | 통과 |
| 시나리오 전환 후 비교값 재계산 | 통과 |

---

## 8. 2026-05-27 검증 기록

| 항목 | 결과 |
|---|---|
| `node --check src/app.js` | 통과 |
| `node tests/verify-data.mjs` | 통과 |
| `python -m json.tool data/phase1-regions.json` | 통과 |
| `python -m json.tool data/skorea-provinces-2018-topo-simple.json` | 통과 |
| 위험지수 산식 패널 표시 | 통과 |
| 폐교 중심 시나리오 전환 후 산식 갱신 | 통과 |

---

## 9. 2026-05-31 검증 기록

| 항목 | 결과 |
|---|---|
| `node --check src/app.js` | 통과 |
| `node tests/verify-data.mjs` | 통과 |
| `node tests/verify-ui-contract.mjs` | 통과 |
| `python -m json.tool data/phase1-regions.json` | 통과 |
| 지역 검색·권역 필터·정렬 | 통과 |
| 대응 전략 브리프 시나리오 갱신 | 통과 |

---

## 10. 향후 자동화 후보

- Lighthouse 기반 접근성 점검
- Playwright 기반 브라우저 렌더링 검증
- 모바일 viewport 스크린샷 비교
- Chart.js 캔버스 렌더링 여부 확인
